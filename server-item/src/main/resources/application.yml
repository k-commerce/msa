spring:
  profiles:
    active: dev

  application:
    name: server-item

  jpa:
    open-in-view: false

server:
  port: 8084

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/
